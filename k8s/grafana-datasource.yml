apiVersion: v1
data:
  datasource.yml: |
    apiVersion: 1

    datasources:
      - name: PostgreSQL
        type: postgres
        access: proxy
        url: db:5432
        database: metricsdb
        user: metrics
        secureJsonData:
          password: metrics
        isDefault: true
        jsonData:
          sslmode: "disable"
kind: ConfigMap
metadata:
  name: grafana-datasources
  namespace: metrics
